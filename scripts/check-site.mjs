import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules'].includes(e.name))continue;const p=path.join(d,e.name);e.isDirectory()?walk(p):files.push(p)}}walk(root);const html=files.filter(f=>f.endsWith('.html'));let errors=[];for(const f of html){const s=fs.readFileSync(f,'utf8');for(const m of s.matchAll(/(?:href|src)="(\/[^"]+)"/g)){let u=m[1].split(/[?#]/)[0];if(u.includes('${')||u.startsWith('//'))continue;let target=path.join(root,u);if(u.endsWith('/'))target=path.join(target,'index.html');if(!fs.existsSync(target))errors.push(`${path.relative(root,f)} -> missing ${u}`)}if(!/<title>/.test(s))errors.push(`${f} missing title`);if(!/name="description"/.test(s))errors.push(`${f} missing description`);if(!/rel="canonical"/.test(s))errors.push(`${f} missing canonical`)}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Checked ${html.length} HTML files: all internal assets and SEO essentials present.`);